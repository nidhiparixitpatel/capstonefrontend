import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  contentContainer: {
    paddingTop: 30,
  },
  infoContainer: {
    paddingTop: 30,
    alignItems: 'center',
    textAlign: "left",
    paddingBottom: 30,
  },
  day: {
    paddingTop: 20
  },
  sync:{
    fontSize: 20,
    paddingBottom: 20
  }
});

export default styles