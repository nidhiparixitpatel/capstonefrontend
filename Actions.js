export const addToken = token => (
  {
    type: 'ADD_TOKEN',
    payload: token,
  }
);

export const addEmail = email => (
  {
    type: 'ADD_EMAIL',
    payload: email,
  }
);

export const addId = id => (
  {
    type: 'ADD_ID',
    payload: id,
  }
);




